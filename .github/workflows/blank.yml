name: CI

on:
  # schedule:
   # - cron: "*/60 * * * *"
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  macos:
    name: macOS on Apple Silicon 😍	
    runs-on: ghcr.io/cirruslabs/macos-ventura-xcode:14.3
    
    steps:
      - uses: actions/checkout@v3
      - name: version
        run: node --version
